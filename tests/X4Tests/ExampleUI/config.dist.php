<?php
/**
 * Template file for the example UI's `config.php` file.
 *
 * @package X4Tests
 * @subpackage ExampleUI
 */

// Absolute URL to the package's root folder in the
// local webserver (without trailing slash).
const X4TESTS_INSTALL_URL = 'http://localhost/x4-core';
